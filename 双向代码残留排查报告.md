# 双向代码残留排查报告

- 日期：2026-08-15
- 范围：`drop_point_prediction/` 与 `hybrid_vs/` 两个方案文件夹
- 性质：纪念性开源整理，目标为「各方案内部自洽、可读、符合规范」，不必追求两方案一致

## 背景与结论

两个文件夹是从「同时含 drop 与 hybrid 代码」的同一个 base 分出来的，因此存在**双向互含**：每个方案里都可能残留另一方案的代码或引用。

实际排查结果：污染以「**hybrid 含 drop 残留**」为主（量大），「**drop 含 hybrid 残留**」很少（仅 2 处）。

> 说明：整理过程中 `hybrid_vs` 侧的 `node_params.yaml` 死配置段、`bringup.launch.py` 启动块已先行清理，本报告按最新状态核对。

---

## 一、hybrid_vs 里需隔绝的 drop 残留

hybrid_vs 是纯 IBVS 方案（detect → ibvs → serial_driver），以下 drop 方案的 planner/tracker/predictor 代码与引用属于残留。

| # | 文件 | 残留内容 |
|---|------|----------|
| 1 | `src/volleyball_robot/package.xml` | 失效依赖 `volleyball_track` / `volleyball_predict` / `volleyball_plan` / `volleyball_pid_camera`（包已删）；**缺 `volleyball_ibvs`** |
| 2 | `src/volleyball_serial_driver/include/volleyball_serial_driver/serial_driver.hpp`<br>`src/volleyball_serial_driver/src/serial_driver.cpp` | plan 模式整条链路：`plan_callback`、`has_new_plan_`、`latest_plan_`、`plan_sub_`(`/planner/plan`)、`reset_tracker_client_`(`/tracker/reset`)、`reset_planner_client_`(`/planner/reset`)；`serial_write_callback` 的 `else if (has_new_plan_)` cmd=0 分支；`robot_callback` 里调用 tracker/planner 复位的模式切换逻辑；`#include "volleyball_interfaces/msg/plan.hpp"` |
| 3 | `src/volleyball_detect/include/volleyball_detect/detector_node.hpp` | 死成员 `pid_camera_pub_` + 死 include `volleyball_interfaces/msg/pid_camera.hpp`（cpp 里从未使用） |
| 4 | `tools/ball_visualizer.py` | 整个脚本是 drop 方案可视化工具：订阅 `/tracker/target`、`/predictor/ball_trajectory`、`BallTrajectory`、EKF residual、轨迹窗口等，hybrid 里这些话题均不存在，脚本基本失效 |
| 5 | `src/volleyball_robot/config/launch_params.yaml` | 失效条目 `pid_camera_node` / `tracker_node` / `predict_node` / `planner_node`（`is_to_launch` 与 `info_level` 两段均有） |
| 6 | `src/volleyball_interfaces/msg/BallTrajectory.msg` | hybrid 里完全无消费者（predictor 已删） |
| 7 | `src/volleyball_robot/rviz/volleyball_robot.rviz` | 引用 `/tracker/marker`、`/planner/marker`、`/predictor/marker`、`ball_trajectory` 显示项 |

## 二、drop_point_prediction 里需隔绝的 hybrid 残留

drop_point_prediction 是完整方案（detect → track → predict → plan → serial_driver），以下 IBVS/pid_camera 残留需清理。

| # | 文件 | 残留内容 |
|---|------|----------|
| 8 | `src/volleyball_serial_driver/include/volleyball_serial_driver/serial_driver.hpp` | 死成员 `has_new_pid_cam_`、`has_pid_cam_data_`（IBVS 标志，cpp 里无 pid_cam 逻辑，从未使用） |
| 9 | `src/volleyball_robot/package.xml` | 失效依赖 `volleyball_pid_camera`（包已从两方案删除） |

---

## 三、两方案正确共有（非污染，保留）

- 共享包：`volleyball_detect` / `volleyball_interfaces` / `volleyball_robot` / `volleyball_robot_description` / `volleyball_serial_driver`
- `strike_point` frame：`robot_description` 的击打点，两方案都用到（hybrid/IBVS 用它做控制参考系，drop 的 planner 也涉及击球点）
- `Ball.msg` 基础字段 `x/y/z/radius/confidence/position_type`；`RobotBase.msg`

---

## 四、边界项（影响面较大，需确认后再动）

- **`Ball.msg` 字段**：
  - `vx/vy/vz`（速度）——仅 drop 的 `tracker`/`predictor` 在写，hybrid 完全不用
  - `du`（像素偏移）——`detector_node.cpp` 有赋值，但 hybrid 里无消费者（旧 `volleyball_pid_camera` 节点已删）
  - interfaces 是共享包，收窄这些字段要单独确认
- **`Plan.msg`**：只被 serial_driver 的 plan 模式使用，若 #2 清理后它也变 dead

---

## 五、附带发现（不算「多余」，建议顺手处理）

- 两个 `package.xml` 的依赖名 `realsense_camera` 疑似笔误，实际 ROS2 包是 `realsense2_camera`（`bringup.launch.py` 里 `get_package_share_directory("realsense2_camera")` 用的即为正确名）

---

## 清理顺序建议

1. 低风险、纯死代码：`#3`（detector_node 死成员）、`#8`（drop 的 IBVS 死成员）、`#1/#9`（package.xml 失效/缺失依赖）、`#5`（launch_params 失效条目）、`#6`（BallTrajectory.msg）、`#7`（rviz 失效显示项）
2. 需用户拍板：`#2`（serial_driver 的 plan 模式，删=纯 IBVS / 留=协议兼容）、`#4`（ball_visualizer.py 是删是改）
3. 附带：`realsense_camera` 依赖名修正