# 双向代码残留排查报告

- 日期：2026-08-15
- 状态：**两方案的双向残留已全部清理**（hybrid_vs 11 项 + drop_point_prediction 2 项 + 接口成员收敛）
- 范围：`drop_point_prediction/` 与 `hybrid_vs/` 两个方案文件夹
- 性质：纪念性开源整理，目标为「各方案内部自洽、可读、符合规范」，不必追求两方案一致

## 背景与结论

两个文件夹是从「同时含 drop 与 hybrid 代码」的同一个 base 分出来的，因此存在**双向互含**：每个方案里都可能残留另一方案的代码或引用。

实际排查结果：污染以「**hybrid 含 drop 残留**」为主，「**drop 含 hybrid 残留**」很少。目前已全部清理。

---

## 一、hybrid_vs 里需隔绝的 drop 残留（已清理）

| # | 文件 | 处理 |
|---|------|------|
| 1 | `src/volleyball_robot/package.xml` | ✓ 删 `volleyball_track/predict/plan/pid_camera` 依赖，补 `volleyball_ibvs` |
| 2 | `src/volleyball_serial_driver/`（.hpp/.cpp/CMakeLists/package.xml） | ✓ 删 plan 模式整条链路 + 连带 `std_srvs` 依赖，只剩 IBVS（cmd=1） |
| 3 | `src/volleyball_detect/include/volleyball_detect/detector_node.hpp` | ✓ 删死成员 `pid_camera_pub_`、`usb_cam_sub_` + `pid_camera.hpp` include |
| 4 | `tools/ball_visualizer.py` | ✓ 删除（drop 有同名副本） |
| 5 | `src/volleyball_robot/config/launch_params.yaml` | ✓ 删 `is_to_launch`/`info_level` 里失效条目 + `usb_camera: false` |
| 6 | `src/volleyball_interfaces/`（BallTrajectory.msg、Plan.msg + CMakeLists） | ✓ 删两个无消费者 msg |
| 7 | `src/volleyball_robot/rviz/volleyball_robot.rviz` | ✓ 删 `/predictor/marker`、`/tracker/marker`、`/planner/marker` 三个显示项 |
| 8 | `src/volleyball_robot/config/camera_params.yaml` + `launch/bringup.launch.py` | ✓ 删 `usb_camera:` 段（旧 pid_camera 节点 USB 相机）+ launch 里 usb_camera 相关注释与死代码 |
| 9 | `src/volleyball_robot/config/camera_extr.txt` | ✓ 删除（死标定记录，代码未引用） |
| 10 | `src/volleyball_robot/config/strike_point.txt` | ✓ 删除（死标定记录，有效值已在 launch_params.yaml） |
| 11 | `src/volleyball_robot/config/node_params.yaml` | ✓ `model_path` 由 `v3_3_int8` 改为仓库实际存在的 `v3_1_int8` |

## 二、drop_point_prediction 里需隔绝的 hybrid 残留（已清理）

| # | 文件 | 处理 |
|---|------|------|
| 12 | `src/volleyball_serial_driver/include/volleyball_serial_driver/serial_driver.hpp` | ✓ 删死成员 `has_new_pid_cam_`、`has_pid_cam_data_` |
| 13 | `src/volleyball_robot/package.xml` | ✓ 删失效依赖 `volleyball_pid_camera` |

## 三、接口成员收敛（已完成）

`Ball.msg` 原含多个两方案均未使用或仅单方案使用的字段，已按实际使用收敛：

- **hybrid_vs `Ball.msg`** → `{header, du, x, y, z}`（`du` 保留，其余 6 个未用字段删除）
- **drop_point_prediction `Ball.msg`** → `{header, x, y, z, vx, vy, vz}`（`vx/vy/vz` 为 tracker/predictor 所用，保留）
- 同步删除 detector_node.cpp 里 `ball_msg.radius = ...` 的赋值（`radius` 两方案均赋值未读）

字段使用情况（清理依据）：

| 字段 | hybrid_vs | drop_point | 处理 |
|---|---|---|---|
| header / x / y / z | 用 | 用 | 保留 |
| du | 赋值（保留） | 无此字段 | hybrid 保留 |
| vx / vy / vz | 未用 | 用（tracker/predictor） | hybrid 删，drop 留 |
| state / radius / confidence / position_type | 未用/赋值未读 | 未用/赋值未读 | 都删 |

> `PidCamera.msg`（pixel_diff_x/y、is_hit）与 `RobotBase.msg`（x/y/self_pitch/self_yaw/mode）字段均在用，未动。

---

## 四、两方案正确共有（非污染，保留）

- 共享包：`volleyball_detect` / `volleyball_interfaces` / `volleyball_robot` / `volleyball_robot_description` / `volleyball_serial_driver`
- `strike_point` frame：`robot_description` 的击打点，两方案都用到
- `RobotBase.msg`；`PidCamera.msg`（仅 hybrid）

## 五、附带发现（未处理，供参考）

- 两个 `package.xml` 的依赖名 `realsense_camera` 疑似笔误，实际 ROS2 包是 `realsense2_camera`（`bringup.launch.py` 里用的即为正确名）
- `tools/test_ball_publisher.py` 有一句注释 `# 阻力参数，匹配tracker的 k=0.20, m=0.27` 提到 tracker（纯注释，不影响功能）
